// const Joi = require('joi');

// const linkSeqSchema = {
//     // _id is the sequence name
//     _id: Joi.array().items(Joi.string().min(3).max(30).required()).min(1).required(),
// };

// module.exports = linkSeqSchema;
